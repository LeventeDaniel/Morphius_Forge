# Output Validator — Example Module

> **This is an example module.**

Validates that LLM text output is valid JSON and optionally checks for required keys.

## Type

`frontend` — pure browser logic, no backend needed.

## Validation

```bash
morphius-forge validate .
```
