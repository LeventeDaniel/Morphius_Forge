// Minimal module entry point — replace with your implementation

export default function MyModule() {
  return (
    <div style={{ padding: 16, color: '#e8e8e8', fontFamily: 'monospace' }}>
      My Module — replace this with your UI
    </div>
  );
}
